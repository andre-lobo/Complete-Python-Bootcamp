# Modules

# Import math module
import math

print(math.sqrt(4))

# Import specific requisite
from math import pi

print(pi)

# Install modules in terminal
# conda install *module name*
# pip install *module name*
# pip install --upgrade *module name*